---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: openjdk
    build: 8-jdk/alpine
    tag: latest

inputs:
- name: repo

run:
  path: repo/gradlew
  args: ["init", "tasks", "--all"]
params:
  TERM: xterm
